### Course project for CS502
This project, USCC, builds out a working compiler for a C language subset called USC. 
USCC uses the LLVM framework as its code generation engine.

#### project 1
Recursive Descent Parsing
